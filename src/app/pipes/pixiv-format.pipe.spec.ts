import { PixivFormatPipe } from './pixiv-format.pipe';

describe('PixivFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PixivFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
