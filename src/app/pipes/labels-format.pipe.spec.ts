import { LabelsFormatPipe } from './labels-format.pipe';

describe('LabelsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
